'use strict'

const TEXT = {
  // Pour l'utiliser : `console.log(locale('property',{variable:"Ma valeur"}))`
  // Retourne dans la console = "Valeur avec Ma valeur"
    'property': "Valeur avec ${variable}"
  , 'autre-property': "Autre propriété sans variable"
  
}
